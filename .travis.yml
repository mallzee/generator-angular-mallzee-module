language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-mallzee-module' ]; then cd .. && eval "mv $currentfolder generator-angular-mallzee-module" && cd generator-angular-mallzee-module; fi

